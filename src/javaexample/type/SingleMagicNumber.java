package javaexample.type;

public class SingleMagicNumber {
    public int Offset;
    public byte[] Bytes;

    public SingleMagicNumber(int offset, byte[] bytes) {
        Offset = offset;
        Bytes = bytes;
    }
}

